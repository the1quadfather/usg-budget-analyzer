# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Raw source documents — re-downloadable via the acquisition pipeline
dod_ic_budget_analyzer/data/raw/

# Logs & scratch
dod_ic_budget_analyzer/logs/
*.log

# Secrets & local config
.env
.env.*

# Tooling
.claude/
.vscode/
.ipynb_checkpoints/

# The database ships COMPRESSED as usg_budgets.db.gz — 124 MB raw is past
# GitHub's 100 MB per-file limit, and it is mostly prose so it stores at about
# a quarter of that. storage.db.get_engine() expands it on first use.
dod_ic_budget_analyzer/data/processed/usg_budgets.db
