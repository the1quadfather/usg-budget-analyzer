polars
sqlalchemy
tqdm
httpx
beautifulsoup4
pdfplumber          # R-1 PDF fallback when poppler's pdftotext is absent
openpyxl
google-genai
altair
rapidfuzz
pyarrow>=8.0.0
pandas
sentence-transformers
# torch: install the CPU build FIRST on any machine that will not use a GPU.
# The default PyPI wheel is the CUDA build -- several GB -- and nothing in the
# app needs it; the model runs on CPU. The Dockerfile already does this, but a
# manual `pip install -r requirements.txt` does not:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
# Listed here so pip treats the requirement as satisfied rather than pulling
# the CUDA wheel over the top of it.
torch
matplotlib
seaborn
xlsxwriter
streamlit